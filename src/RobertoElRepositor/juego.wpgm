import wollok.game.*
import config.*
import niveles.*
import menuYExtras.*

program juego {
	game.width(config.ancho())
	game.height(config.alto())
	game.cellSize(64)
	game.title("Tito el MueveCajas")
	game.boardGround("roberto/piso_bloque.png")
	config.configurarTeclas()
	menu.ejecutar()
	game.start()
}