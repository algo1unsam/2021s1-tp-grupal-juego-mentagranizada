import wollok.game.*
import config.*
import niveles.*
import menuYExtras.*

program juego {
	game.width(config.ancho())
	game.height(config.alto())
	game.cellSize(64)
	game.title("Pepe el MueveCajas")
	game.boardGround("Bloques/piso_bloque.png")
	config.configurarTeclas()
	menu.ejecutar()
	game.start()
	
} 