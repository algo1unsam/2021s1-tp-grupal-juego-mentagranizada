import wollok.game.*
import config.*
import niveles.*
import extras.*

program juego {
	game.width(config.ancho())
	game.height(config.alto())
	game.cellSize(64)
	game.title("Pepe el MueveCajas")
	game.boardGround("piso_bloque.png")
	config.configurarTeclas()
	menu.ejecutar()
	game.start()
}
